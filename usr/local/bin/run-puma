#!/bin/bash
app=$1; config=$2; log=$3;
# Formerly 2>&1 >> $log, but that didn't actually capture all output.
echo "starting puma as `whoami`: cd $app && bundle exec puma -C $config &>> $log" >> $log
cd $app && /usr/local/rvm/bin/rvm default do bundle exec puma -C $config &>> $log